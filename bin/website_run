#!/bin/bash

set -Eeuo pipefail

# Clean
rm -rf website/html/

# Build
website_generator website

if [ -z ${1+x} ]; then 
    # Serve
    cd website/html
    python3 -m http.server 8000
else
    echo "Rebuild site"
fi
